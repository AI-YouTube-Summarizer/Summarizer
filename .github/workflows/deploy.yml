#!/bin/bash
# setup.sh - Script to install dependencies

# Install system dependencies (xclip)
sudo apt-get update
sudo apt-get install -y xclip

# Install Python dependencies
pip install -r requirements.txt
